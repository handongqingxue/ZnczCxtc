<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.znczCxtc.dao.CheLiangTaiZhangMapper">
	<insert id="uploadJinChang" parameterType="com.znczCxtc.entity.CheLiangTaiZhang">
		insert into che_liang_tai_zhang
		<trim prefix="(" suffix=")" suffixOverrides=",">
				jcsj,
			<if test="jczp!=null">
				jczp,
			</if>
			<if test="ddId!=null">
				ddId,
			</if>
		</trim>
		values
		<trim prefix="(" suffix=")" suffixOverrides=",">
				now(),
			<if test="jczp!=null">
				#{jczp},
			</if>
			<if test="ddId!=null">
				#{ddId},
			</if>
		</trim>
	</insert>
	<update id="uploadChuChang" parameterType="com.znczCxtc.entity.CheLiangTaiZhang">
		update che_liang_tai_zhang
		<trim suffixOverrides=",">
				ccsj=now(),
			<if test="cczp!=null">
				cczp=#{cczp},
			</if>
		</trim>
		 where ddId=#{ddId}
	</update>
	<select id="queryForInt" resultType="java.lang.Integer">
		select count(*) from che_liang_tai_zhang cltz left join ding_dan dd on cltz.ddId=dd.id left join ding_dan_zhuang_tai ddzt on dd.ddztId=ddzt.id left join che_liang cl on dd.cyclId=cl.id
	</select>
	<select id="queryList" resultType="com.znczCxtc.entity.CheLiangTaiZhang">
		select cltz.*,dd.ddh,ddzt.mc ddztMc,cl.cph cyclCph from che_liang_tai_zhang cltz left join ding_dan dd on cltz.ddId=dd.id left join ding_dan_zhuang_tai ddzt on dd.ddztId=ddzt.id left join che_liang cl on dd.cyclId=cl.id
	</select>
</mapper>